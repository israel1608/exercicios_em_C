#include <stdio.h>
#include <stdlib.h>

main(){
    printf("quantidade de bytes de int: %d bytes\n",sizeof(int));
    printf("quantidade de bytes de char: %d bytes\n",sizeof(char));
    printf("quantidade de bytes de float: %d bytes\n",sizeof(float));
    printf("quantidade de bytes de double: %d bytes\n",sizeof(double));
}